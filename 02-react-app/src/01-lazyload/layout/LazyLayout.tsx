import { Navigation } from '../router/Navigation';

export const LazyLayout = () => {
    return (
        <div>
            <h1>Lazy layout - MainPage</h1>
            <Navigation/>
        </div>
    )
}

export default LazyLayout;